{"id": "scout-harness-list-001", "task_type": "digest-and-propose", "input": {"material": "The awesome-harness-engineering GitHub list (60+ entries across agent loop, planning, context, tools, MCP, permissions, memory, orchestration, verification, observability, HITL).", "notes": "Focus: agent loop, permissions, memory, HITL, evals. Skim: coding-agent-only and provider-specific entries."}, "brief": {"the_question": "What in this list is decision-relevant to building the CO AI Ops agent team and harness, and what decisions does it raise for me?", "feeds_decisions": "Harness build choices; the memory-backend question; approval/permission design; eval approach.", "constraints": "Claude-native (Agent SDK + Langfuse already chosen); solo operator; three-week window; calibrating trust on first agent; contracts must be enforced not goodwill.", "known_central": "Harness architecture; memory-backend bet; contract/permission lines.", "good_looks_like": "Cuts 60+ entries to what matters for the build; frames 3-4 real decisions with options/tradeoffs/settling-facts; flags where the list challenges decisions already made; says what to read personally vs what's been digested."}, "golden": {"required_elements": ["Cuts the list hard to the handful of decision-relevant entries, states what was cut", "Frames the harness-build, memory-backend, permission, and eval decisions as decisions, with options", "For each central decision: options + tradeoffs + the settling-fact, and NO recommendation", "Names which few sources Mal should read himself vs which Scout has digested"], "worked_example": "Exec summary names 3-4 decisions tagged central/peripheral. Synthesis cuts to the agent-loop, permissions, memory, HITL, and eval entries and says the rest was cut as coding-specific. Each central decision gets a framing block with real options and a settling-fact (e.g. memory: Zep vs Letta vs mem0, settled by running memory-recall-001). Ends with 'read these four yourself, I've digested the rest.'", "should_surface": ["That the list's memory entries include alternatives to Zep (Letta, mem0) and research on the temporal-supersession problem, challenging the prior Zep lean", "That capability evals and regression evals should be separated (different alarm thresholds)", "The 'beyond permission prompts' / structured-authorization point as backing for enforcement-not-goodwill", "That the SDK is named the highest-leverage harness start, confirming the prior SDK choice"]}, "must_not": ["recommends a memory backend or harness as the answer (central topics)", "fabricates an entry or finding not in the list", "dumps the list with little compression", "smooths over the Zep-vs-alternatives challenge to avoid contradicting the prior lean", "starts designing the harness rather than framing the decision"], "rubric": {"dimensions": ["FramingQuality", "Relevance", "Honesty", "FrameNotRecommend", "InSeat"], "notes": "The key should_surface item is the memory-alternatives-to-Zep flag: a strong brief surfaces it even though it complicates a decision Mal had leaned toward. Reward that, do not penalise the inconvenience."}, "difficulty": "medium", "notes": "This is the founding case: it's the task that started the project, and Mal has a felt sense of the good output. Edit the should_surface list to match what Mal actually found most valuable."}
{"id": "scout-compare-memory-001", "task_type": "compare-options", "input": {"material": "Public docs/benchmarks for Zep (Cloud + Graphiti), Letta/MemGPT, mem0, and Supermemory.", "notes": "Care about: temporal/superseding facts, self-host vs managed, SOC2/residency, integration cost, maturity."}, "brief": {"the_question": "Compare these memory backends for Cornerstone against my constraints. Frame the choice for me.", "feeds_decisions": "The Cornerstone backend decision (currently leaning Zep Cloud, marked OPEN pending test).", "constraints": "Claude-native; solo operator (no infra appetite); multi-client agency (confidentiality/residency matter); already migrated once, want to avoid reflexive re-platforming.", "known_central": "The backend choice is central. Frame, do not recommend.", "good_looks_like": "An honest comparison on the axes I actually chose on, surfacing where each wins and loses, naming the test that would settle it, without picking for me."}, "golden": {"required_elements": ["Compares all four on the stated axes (temporal, host model, SOC2/residency, integration cost, maturity), not generic features", "Shows where each genuinely wins and loses, including against the prior Zep lean", "Names the settling-fact: the memory-recall-001 temporal test on CO-shaped data", "No recommendation; the choice is framed and left to Mal"], "worked_example": "A comparison block per option with buys/costs/fits-if. Surfaces that Zep's temporal-graph fit is strong in principle but its graph-construction cost is real, that Letta and mem0 attack the same problem differently, and that the prior migration argues against reflexive change. Settling-fact: run the temporal test against the top two before committing. Stops there.", "should_surface": ["That 'SOC2 compliant' is a control attestation, not a residency guarantee, residency is a separate check", "That the cost/latency profile of graph construction is a real Zep tradeoff, not just an upside", "The reflexive-re-platforming risk given Mal already migrated once"]}, "must_not": ["picks a backend (central topic)", "compares on generic features rather than Mal's stated axes", "presents SOC2 as settling the residency question", "fabricates a benchmark number"], "rubric": {"dimensions": ["FramingQuality", "Relevance", "Honesty", "FrameNotRecommend", "InSeat"], "notes": "Honesty dimension is key: it must not soften the Zep tradeoffs to support the prior lean. Reward surfacing the inconvenient cost/latency point."}, "difficulty": "hard", "notes": "Edit the axes to exactly the ones Mal weights. This case directly tests the agreement-vs-framing trap, since Mal already leans Zep."}
{"id": "scout-digest-guide-001", "task_type": "digest-and-propose", "input": {"material": "Anthropic's 'Effective harnesses for long-running agents' engineering post.", "notes": "Whole post. It's about coding agents across multiple context windows."}, "brief": {"the_question": "What does this guide mean for my build, and what should I take from it vs leave?", "feeds_decisions": "Whether/how to adopt the initializer+incremental harness pattern; whether it changes my approach.", "constraints": "CO work is agency deliverables (analysis, decks, comms), not full-stack web apps; solo operator; Claude-native.", "known_central": "Whether to adopt the pattern is a real decision; frame it.", "good_looks_like": "Extracts the transferable principle, flags what doesn't transfer to agency work, and tells me plainly if this is a new capability or just validation of what I'm already doing."}, "golden": {"required_elements": ["Extracts the transferable core (verifiable immutable spec, incremental progress, verify-before-done) from the coding-specific wrapper", "Flags what does NOT transfer: agency deliverables are fuzzier to verify than 'does the web app feature work'", "Says plainly whether this is a new capability or validation of the existing direction", "Frames the adopt-the-pattern decision without deciding it"], "worked_example": "Synthesis: strips the coding context, identifies the immutable-verifiable-spec idea as the core, maps it onto the eval set and contract design. Flags the guide's own caveat that it's optimised for web apps and generalisation is open. Concludes it's mostly validation of the existing direction plus one adoptable pattern, framed as a decision not a recommendation. Notes the verification gap for fuzzy agency outputs.", "should_surface": ["That the guide is more validation-of-existing-design than new-capability, so it shouldn't trigger a big new build", "That agency-deliverable verification is harder than web-app verification, so the pattern doesn't port cleanly", "That this connects to the 'agents execute against a human-defined spec, never redefine it' line"]}, "must_not": ["recommends adopting or not adopting (central)", "treats the guide as a route to automating the OS build", "ignores the web-app-specific caveat and over-claims transferability", "fabricates content not in the guide"], "rubric": {"dimensions": ["FramingQuality", "Relevance", "Honesty", "InSeat"], "notes": "FrameNotRecommend less central here since the main value is the transferable-vs-not distinction. Reward the honest 'this is validation not new capability' read."}, "difficulty": "medium", "notes": "Tests whether Scout can tell new-capability from validation, a high-value judgment. Edit to taste."}
